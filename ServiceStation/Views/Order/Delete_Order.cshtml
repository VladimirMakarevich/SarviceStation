@model ServiceStation.Models.OrderViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@ViewBag.Message
<div class="container-fluid">
    <!-- Page Heading -->
    <div class="row">
        <div class="col-lg-12">
            <h3>Are you sure you want to delete this?</h3>
            <div>
                <h4>Orders</h4>
                <hr />
                <dl class="dl-horizontal">
                    <dt>
                        @Html.DisplayNameFor(m => m.Cars.Make)
                    </dt>

                    <dd>
                        @Html.DisplayFor(m => m.Cars.Make)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(m => m.Clients.FirstName)
                    </dt>

                    <dd>
                        @Html.DisplayFor(m => m.Clients.FirstName)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(m => m.Date)
                    </dt>

                    <dd>
                        @Html.DisplayFor(m => m.Date)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(m => m.OrderAmount)
                    </dt>

                    <dd>
                        @Html.DisplayFor(m => m.OrderAmount)
                    </dd>

                    <dt>
                        @Html.DisplayNameFor(m => m.Status)
                    </dt>

                    <dd>
                        @Html.DisplayFor(m => m.Status)
                    </dd>

                </dl>

                @using (Html.BeginForm())
                {
                    @Html.AntiForgeryToken()

                    <div class="form-actions no-color">
                        <a href="@Url.Action("List_Order", "Order")" class="btn btn-info btn-sm"> Back to list of order</a>
                        |
                        <input type="submit" value="Delete" class="btn btn-danger btn-sm" />
                    </div>
                }
            </div>
        </div>
    </div>
</div>