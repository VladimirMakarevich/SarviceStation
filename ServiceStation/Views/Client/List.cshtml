@model ServiceStation.Models.ClientListViewModel
@{
    ViewBag.Title = "The list of client cards";
    ViewBag.Subheading = "(last cards)";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <!-- Page Heading -->
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">
                @ViewBag.Title
                <small>@ViewBag.Subheading</small>
            </h1>
            <table class="table table-bordered">
                <!-- On rows -->
                <tr>
                    <th>Id</th>
                    <th class="warning">First Name</th>
                    <th>Last Name</th>
                    <th class="warning">Phone</th>
                    <th>Email</th>
                    <th class="warning">Action</th>
                </tr>
                <!-- On cells (`td` or `th`) -->
                @foreach (var m in Model.ClientCards)
                {
                    <tr>
                        <td>
                            <strong>@m.ClientId</strong>
                        </td>
                        <td class="info">@m.FirstName</td>
                        <td>@m.LastName</td>
                        <td class="info">@m.Phone</td>
                        <td>@m.Email</td>
                        <td>
                            <a href="@Url.Action("Modified", "Client", new { id = m.ClientId })" class="btn btn-info btn-sm">Modified</a>
                            <a href="@Url.Action("Delete", "Client", new { id = m.ClientId })" class="btn btn-warning btn-sm">Delete</a>
                        </td>
                    </tr>
                }
            </table>
        </div>
    </div>
    <!-- /.row -->
</div>
<!-- /.container-fluid -->